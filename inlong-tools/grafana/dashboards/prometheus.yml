apiVersion: 1

datasources:
- name: Agent
  type: prometheus
  access: proxy
  url: ${AGENT_URL}
  isDefault: true
  editable: false
- name: Dataproxy
  type: prometheus
  access: proxy
  url: ${DP_URL}
  isDefault: false
  editable: false
